import 'package:flutter/material.dart';

class OrgMain extends StatelessWidget {
  const OrgMain({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Hello Organizer'));
  }
}
